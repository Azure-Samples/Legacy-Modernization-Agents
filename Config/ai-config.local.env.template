# =============================================================================
# LOCAL OVERRIDES (Private)
# =============================================================================
# Rename this file to 'ai-config.local.env'.
# These values will override the defaults in ai-config.env.
# =============================================================================

# 1. Credentials (Required)
_MAIN_ENDPOINT="https://your-resource.openai.azure.com/"
_MAIN_API_KEY="your-key-here"

# 2. Model Selection (Optional Overrides)
# _CHAT_MODEL="gpt-5.2-chat"
# _CODE_MODEL="gpt-4o"

# 3. Context Window (Optional)
# Set explicit size to force Whole-Program Analysis
# AZURE_OPENAI_CONTEXT_WINDOW_SIZE="128000"
