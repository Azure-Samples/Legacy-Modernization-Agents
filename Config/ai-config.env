# =============================================================================
# Azure OpenAI Configuration Template
# =============================================================================
# This template provides default values used by the tooling. It is safe to keep
# committed to source control because it only contains placeholders. Copy the
# relevant settings into ai-config.local.env (gitignored) with your real
# credentials before running the migration workflow.
# =============================================================================

# Azure OpenAI Service Configuration
AZURE_OPENAI_ENDPOINT="https://g-openai.cognitiveservices.azure.com/"
AZURE_OPENAI_API_KEY="e9e0fcb483f6493fab70d6fb008e936a"

# Model Deployment Configuration - gpt-5.1-codex-mini
AZURE_OPENAI_DEPLOYMENT_NAME="gpt-5.1-codex-mini"
AZURE_OPENAI_MODEL_ID="gpt-5.1-codex-mini"

# Specialized Model IDs (all using same deployment)
AZURE_OPENAI_COBOL_ANALYZER_MODEL="gpt-5.1-codex-mini"
AZURE_OPENAI_JAVA_CONVERTER_MODEL="gpt-5.1-codex-mini"
AZURE_OPENAI_DEPENDENCY_MAPPER_MODEL="gpt-5.1-codex-mini"
AZURE_OPENAI_UNIT_TEST_MODEL="gpt-5.1-codex-mini"

# Service Configuration
AZURE_OPENAI_SERVICE_TYPE="AzureOpenAI"
AZURE_OPENAI_MAX_TOKENS="16384"
AZURE_OPENAI_TEMPERATURE="0.1"

# Application Settings
COBOL_SOURCE_FOLDER="source"
JAVA_OUTPUT_FOLDER="output/java"
CSHARP_OUTPUT_FOLDER="output/csharp"
TEST_OUTPUT_FOLDER="TestOutput"
# NOTE: TARGET_LANGUAGE is set by doctor.sh menu selection - do NOT set it here
# TARGET_LANGUAGE="Java"  # REMOVED - causes bug when user selects C#

# Logging Configuration
LOG_LEVEL="Information"
ENABLE_CHAT_LOGGING="true"
ENABLE_API_CALL_LOGGING="true"

# =============================================================================
# Security Notes:
# - Never commit real API keys.
# - Place sensitive values in Config/ai-config.local.env instead.
# - Consider using Azure Key Vault for production scenarios.
# =============================================================================
